# Projeto 04: Script DDL de Criação 🧱

**Objetivo**: Transformar o seu modelo de dados em código SQL executável.

---

## 🚀 Tarefas
1.  Abra o Query Tool no pgAdmin.
2.  Escreva o script `CREATE TABLE` para o sistema de Escola planejado no [Projeto 02](projeto-02.md).
3.  Inclua:
    *   Chaves Primárias (`SERIAL`).
    *   Chaves Estrangeiras (`REFERENCES`).
    *   Restrições `NOT NULL` e `UNIQUE`.
4.  Crie um **Índice** para a coluna "Nome do Aluno".

## ✅ Critérios de Entrega
*   O arquivo `.sql` contendo todos os comandos de criação.
*   Print da aba "Tables" do pgAdmin mostrando as tabelas criadas.